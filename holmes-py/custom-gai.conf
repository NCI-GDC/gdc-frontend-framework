# Custom /etc/gai.conf to prefer IPv4 addresses over IPv6 for address selection
# This increases the precedence of IPv4-mapped addresses so getaddrinfo prefers A records.
# See 'man gai.conf' for details.

# Give IPv4-mapped addresses higher precedence
precedence ::ffff:0:0/96  100
import React, { FC } from "react";
import IDCViewerWrapper from "../proteinpaint/idc/IDCViewerWrapper";

const IDCViewerApp: FC = () => {
  return <IDCViewerWrapper />;
};

export default IDCViewerApp;

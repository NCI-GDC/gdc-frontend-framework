import { FC } from "react";
import { CorrelationWrapper } from "../proteinpaint/CorrelationWrapper";

const CorrelationApp: FC = () => {
  return <CorrelationWrapper />;
};

export default CorrelationApp;
